# Wiki Generator - Claude Code 插件

> 自动生成和维护项目 Wiki 文档的 Claude Code 插件（v3.0）

[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Version](https://img.shields.io/badge/version-3.0.0-blue.svg)](https://github.com/user/repo-wiki)

## 📖 简介

Wiki Generator v3.0 是一个强大的 Claude Code 插件,能够自动分析代码库并生成高质量的技术文档。

### ✨ v3.0 核心特性

- 🚀 **原生 Claude Code 集成** - 通过 `/plugin marketplace add` 一键安装
- 🎯 **配置驱动** - 通过 `wiki-config.json` 灵活控制生成行为
- 🔍 **技术栈显式检测** - 基于检测到的框架/库生成条件文档
- 🇨🇳 **中文优先** - 支持中文文件名和模板,完整的中文本地化
- 📂 **分层目录结构** - 按功能模块组织文档
- ✅ **完全覆盖策略** - 每次重新生成整个文档,通过 Git 管理版本
- 🛡️ **部分成功机制** - 保留成功生成的文档,跳过失败的
- ✅ **质量验证** - 基于自动化验证确保文档质量
- 🎨 **可定制模板** - 支持自定义文档模板

### 🎯 成功指标

- 减少 **70%** 的文档维护工作量
- 文档质量分数 ≥ **80 分**
- API 文档覆盖率 ≥ **85%**
- 模块文档覆盖率 ≥ **90%**
- 代码示例准确率 ≥ **95%**

## 📦 快速开始

### 安装插件

```bash
# 通过 Marketplace 安装（推荐）
/plugin marketplace add https://github.com/user/repo-wiki

# 或本地安装
/plugin marketplace add /path/to/repo-wiki
```

### 配置

在项目根目录创建 `wiki-config.json`:

```json
{
  "output_dir": "docs",
  "language": "zh",
  "structure_template": "reference",
  "include_sources": true,
  "generate_toc": true
}
```

### 生成文档

```bash
# 生成完整 Wiki
/wiki-generate --full

# 生成项目概览
/wiki-generate --overview

# 生成特定模块
/wiki-generate --module src/utils
```

## 🚀 使用指南

### 命令参数

| 参数 | 简写 | 说明 |
|------|------|------|
| `--full` | `-f` | 生成完整 Wiki（概览 + 模块 + API） |
| `--overview` | `-o` | 仅生成项目概览 |
| `--module` | `-m` | 生成指定模块文档 |
| `--api` | `-a` | 生成 API 文档 |
| `--output` | `-d` | 指定输出目录 |
| `--force` | `-f` | 强制覆盖已存在的文件 |
| `--verbose` | `-v` | 显示详细输出 |

### 配置选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `output_dir` | string | `"docs"` | 文档输出目录 |
| `language` | string | `"zh"` | 文档语言（`"zh"`/`"en"`/`"both"`） |
| `structure_template` | string | `"reference"` | 结构模板（`"reference"`/`"simple"`） |
| `include_sources` | boolean | `true` | 是否包含源文件引用 |
| `generate_toc` | boolean | `true` | 是否生成目录索引 |
| `exclude_patterns` | array | `[]` | 排除的文件/目录模式 |
| `quality_threshold` | number | `80` | 文档质量分数阈值 |

### 技术栈检测

| 检测规则 | 触发条件 | 生成文档 |
|---------|---------|---------|
| SQLAlchemy | `from sqlalchemy` 或 `import sqlalchemy` | 数据模型/数据模型.md |
| Django ORM | `from django.db` | 数据模型/数据模型.md |
| FastAPI | `from fastapi` 或 `import fastapi` | API 文档/API 接口.md |
| Flask | `from flask` | API 文档/API 接口.md |
| Celery | `from celery` 或 `import celery` | 任务队列/任务队列.md |
| pytest | `import pytest` | 测试策略.md |
| Dockerfile | 文件存在 | 部署指南.md |

## 📁 插件结构

```
repo-wiki/
├── .claude-plugin/              # 插件根目录
│   ├── marketplace.json         # Marketplace 配置
│   ├── plugin.json              # 插件清单
│   ├── README.md                # 插件文档
│   ├── commands/                # 命令定义
│   │   └── wiki-generate.md     # Wiki 生成命令
│   └── templates/               # 文档模板
│       ├── base-docs/           # 基础文档模板
│       ├── modules/             # 模块文档模板
│       └── tech-specific/       # 技术特定模板
└── README.md                    # 本文件
```

## 📂 生成的文档结构

```
docs/
└── zh/                          # 语言目录
    └── content/                 # 内容目录
        ├── 00-快速开始.md        # 必需文档（10 个）
        ├── 01-项目概述.md
        ├── 02-技术栈与依赖.md
        ├── 03-系统架构设计.md
        ├── 数据模型/             # 条件文档（检测到 SQLAlchemy）
        │   └── 数据模型.md
        ├── API 文档/             # 条件文档（检测到 FastAPI）
        │   └── API 接口.md
        ├── 04-核心功能.md
        ├── 05-开发指南.md
        ├── 06-部署指南.md
        ├── 07-测试策略.md
        ├── 08-故障排除.md
        └── 09-安全考虑.md
```

## 💡 使用示例

### 示例 1: 生成完整 Wiki

```bash
/wiki-generate --full
```

生成项目概览、所有模块文档和 API 文档。

### 示例 2: 生成特定模块

```bash
/wiki-generate --module src/utils
```

只生成 `src/utils` 模块的文档。

### 示例 3: 更新 API 文档

```bash
/wiki-generate --api --force
```

强制更新所有 API 文档。

### 示例 4: 自定义输出目录

```bash
/wiki-generate --full --output documentation
```

将生成的文档保存到 `documentation/` 目录。

## 🎨 模板系统

插件使用模板系统生成文档,默认模板位于 `.claude-plugin/templates/`:

### 必需文档（10 个）

1. 快速开始（quickstart.md.template）
2. 项目概述（overview.md.template）
3. 技术栈与依赖（techstack.md.template）
4. 系统架构设计（architecture.md.template）
5. 核心功能（corefeatures.md.template）
6. 开发指南（development.md.template）
7. 部署指南（deployment.md.template）
8. 测试策略（testing.md.template）
9. 故障排除（troubleshooting.md.template）
10. 安全考虑（security.md.template）

### 条件文档（根据技术栈生成）

- 数据模型（datamodel.md.template）- 检测到 SQLAlchemy/Django ORM
- API 接口（api.md.template）- 检测到 FastAPI/Flask
- 任务队列（taskqueue.md.template）- 检测到 Celery/RQ

### 自定义模板

1. 在项目根目录创建 `templates/` 目录
2. 复制默认模板到自定义目录
3. 修改模板文件
4. 在 `wiki-config.json` 中配置模板路径

## 🔧 故障排除

### 问题: 插件无法加载

**解决方案**:
1. 检查 `.claude-plugin/` 目录结构是否正确
2. 验证 `marketplace.json` 和 `plugin.json` 格式
3. 确认命令文件路径使用相对路径（`./` 开头）

### 问题: 命令无法执行

**解决方案**:
1. 检查命令文件的 frontmatter 格式
2. 确认 `wiki-config.json` 配置正确
3. 使用 `--verbose` 查看详细错误信息

### 问题: 文档生成不完整

**解决方案**:
1. 检查代码注释是否完整
2. 确认 `exclude_patterns` 没有排除必要文件
3. 使用 `--force` 强制重新生成

更多问题请查看 [.claude-plugin/README.md](.claude-plugin/README.md)

## 📊 文档质量评分

插件会为生成的文档打分（0-100 分）:

- **≥ 90 分**: 优秀 - 文档完整且详细
- **80-89 分**: 良好 - 文档基本完整
- **70-79 分**: 一般 - 文档部分缺失
- **< 70 分**: 需要改进 - 文档不完整

可以通过 `quality_threshold` 配置项设置最低质量要求。

## 🎯 最佳实践

1. **定期更新文档**: 在代码变更后运行 `/wiki-generate --full`
2. **使用版本控制**: 将生成的文档纳入 Git 管理
3. **自定义模板**: 根据项目需求定制文档模板
4. **质量检查**: 定期检查文档质量分数,保持 ≥ 80 分
5. **排除不必要的文件**: 合理配置 `exclude_patterns` 避免生成无关文档

## 🆚 版本对比

### v2.0 → v3.0 重大变更

| 特性 | v2.0 | v3.0 |
|------|------|------|
| 安装方式 | `uvx wiki-generator` | `/plugin marketplace add` |
| 包类型 | Python 包 | Claude Code 插件 |
| Python 依赖 | 需要 | 不需要 |
| 安装复杂度 | 中等（需要 Python 环境） | 简单（原生 Claude Code） |
| 项目结构 | wiki_generator/ + CLI | .claude-plugin/ |
| 配置文件 | pyproject.toml | plugin.json + marketplace.json |
| 文档生成 | ✅ | ✅（100% 兼容） |
| 模板系统 | ✅ | ✅（100% 兼容） |

### ⚠️ Breaking Changes

- **不再支持** `uvx wiki-generator` CLI 安装方式
- **不再包含** Python 包和 CLI 工具
- **必须使用** Claude Code 插件系统安装

### ✅ 兼容性

- 配置文件格式 **100% 兼容**
- 文档模板 **100% 兼容**
- 生成文档质量 **100% 兼容**
- 所有功能特性完全保留

## 🔗 相关链接

- [Claude Code](https://claude.ai/code) - 官方 Claude Code 文档
- [.claude-plugin/README.md](.claude-plugin/README.md) - 详细插件文档
- [许可证](LICENSE) - MIT License

## 🤝 贡献

欢迎贡献！请查看项目 Issues 了解当前开发方向。

## 📄 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- 感谢 Anthropic 团队提供 Claude Code
- 感谢所有贡献者的支持

---

**版本**: 3.0.0
**最后更新**: 2025-01-06
