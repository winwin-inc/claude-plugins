# Save Session Command

请执行以下任务来保存当前会话的计划和执行结果:

## 1. 创建保存目录结构

首先创建必要的目录:
```bash
mkdir -p docs/claude-sessions/$(date +%Y%m)
mkdir -p docs/plans
mkdir -p docs/execution-logs
```

## 2. 保存计划文档

如果存在 `plan.md` 或 `PLAN.md` 文件,请将其保存到:
- 目标路径: `docs/plans/<serial>-<summary>.md` 自动生成序号和简短 summary
- 在文件开头添加元数据:
  ```markdown
  ---
  created: $(date +%Y-%m-%d %H:%M:%S)
  session_id: [当前会话标识]
  status: completed
  ---
  ```

## 3. 生成执行摘要

创建文件 `docs/claude-sessions/$(date +%Y%m)/session_$(date +%Y%m%d_%H%M%S).md`,包含:

### 会话信息
- 执行时间: $(date +%Y-%m-%d %H:%M:%S)
- 工作目录: $(pwd)
- Git 分支: $(git branch --show-current)

### 任务概述
[请总结本次会话完成的主要任务,包括:]
- 主要目标
- 实施的关键步骤
- 使用的技术和工具

### 文件变更
```bash
# 列出所有修改的文件
git status --short

# 显示变更统计
git diff --stat
```

### 执行结果
[请描述:]
- ✅ 成功完成的任务
- ⚠️  遇到的问题及解决方案
- 📝 待办事项或后续建议
- 🧪 测试结果(如果有)

### 关键代码片段
[如果有重要的代码变更,摘录关键部分并说明其作用]

## 4. 创建执行日志

将以下信息追加到 `docs/execution-logs/history.log`:
```
========================================
Session: $(date +%Y-%m-%d %H:%M:%S)
----------------------------------------
Branch: $(git branch --show-current)
Files Changed: $(git status --short | wc -l)
Commits: $(git log --oneline -1)
Status: ✅ Completed
========================================
```

## 5. 生成可读性报告

创建 `docs/claude-sessions/$(date +%Y%m)/README.md`,列出该月所有会话:
- 按时间倒序排列
- 显示每个会话的标题和状态
- 提供快速导航链接

## 7. 确认保存成功

最后,请输出:
```
🎉 会话保存完成!

📁 保存位置:
  - 计划文档: docs/plans/<serial>-<summary>.md
  - 会话摘要: docs/claude-sessions/[YYYYMM]/session_[timestamp].md
  - 执行日志: docs/execution-logs/history.log

📊 统计信息:
  - 文件修改: [数量] 个
  - 新增行数: [数量]
  - 删除行数: [数量]
```