# Wiki Generator - Claude Code 插件

自动生成和维护项目 Wiki 文档的 Claude Code 插件。

## 快速开始

### 安装插件

```bash
# 通过 Marketplace 安装
/plugin marketplace add https://github.com/user/repo-wiki

# 或本地安装
/plugin marketplace add /path/to/repo-wiki
```

### 使用

```bash
# 生成完整 Wiki
/wiki-generate --full

# 生成特定模块
/wiki-generate --module src/utils

# 生成项目概览
/wiki-generate --overview
```

## 配置

在项目根目录创建 `wiki-config.json`:

```json
{
  "output_dir": "docs",
  "exclude_patterns": ["node_modules", "dist", ".git"],
  "template_dir": ".claude-plugin/templates",
  "quality_threshold": 80,
  "diagrams": {
    "enabled": true,
    "detail_level": "medium"
  }
}
```

### 配置选项

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `output_dir` | string | `"docs"` | Wiki 文档输出目录 |
| `exclude_patterns` | array | `[]` | 排除的文件/目录模式 |
| `template_dir` | string | `.claude-plugin/templates` | 模板目录路径 |
| `quality_threshold` | number | `80` | 文档质量分数阈值 |
| `diagrams.enabled` | boolean | `true` | 是否生成架构图 |
| `diagrams.detail_level` | string | `"medium"` | 图表详细程度 (low/medium/high) |

## 功能特性

- 🚀 **自动生成项目概览文档** - 自动识别项目结构和关键信息
- 📦 **模块文档自动提取** - 从代码注释生成模块文档
- 🔧 **技术栈识别** - 自动检测项目使用的技术栈
- 📊 **API 文档生成** - 生成 API 参考文档
- 🎨 **可定制模板** - 支持自定义文档模板
- 📈 **质量评分** - 文档完整性评分系统

## 命令参数

### wiki-generate

| 参数 | 简写 | 说明 |
|------|------|------|
| `--full` | `-f` | 生成完整 Wiki (概览 + 模块 + API) |
| `--overview` | `-o` | 仅生成项目概览 |
| `--module` | `-m` | 生成指定模块文档 |
| `--api` | `-a` | 生成 API 文档 |
| `--output` | `-d` | 指定输出目录 |
| `--force` | `-f` | 强制覆盖已存在的文件 |
| `--verbose` | `-v` | 显示详细输出 |

## 使用示例

### 示例 1: 生成完整 Wiki

```bash
/wiki-generate --full
```

生成项目概览、所有模块文档和 API 文档。

### 示例 2: 生成特定模块

```bash
/wiki-generate --module src/utils
```

只生成 `src/utils` 模块的文档。

### 示例 3: 更新 API 文档

```bash
/wiki-generate --api --force
```

强制更新所有 API 文档。

### 示例 4: 自定义输出目录

```bash
/wiki-generate --full --output documentation
```

将生成的文档保存到 `documentation/` 目录。

## 模板系统

插件使用模板系统生成文档,默认模板位于 `.claude-plugin/templates/`:

- `base-docs/` - 基础文档模板
- `modules/` - 模块文档模板
- `tech-specific/` - 技术特定模板

### 自定义模板

1. 在项目根目录创建 `templates/` 目录
2. 复制默认模板到自定义目录
3. 修改模板文件
4. 在 `wiki-config.json` 中设置 `template_dir`

## 技术栈识别

插件可以自动识别以下技术栈:

- **前端**: React, Vue, Angular, Svelte, etc.
- **后端**: Node.js, Python, Java, Go, Rust, etc.
- **框架**: Express, Django, Flask, Spring, etc.
- **数据库**: PostgreSQL, MySQL, MongoDB, Redis, etc.
- **工具**: Docker, Kubernetes, CI/CD, etc.

## 文档质量评分

插件会为生成的文档打分 (0-100 分):

- **≥ 90 分**: 优秀 - 文档完整且详细
- **80-89 分**: 良好 - 文档基本完整
- **70-79 分**: 一般 - 文档部分缺失
- **< 70 分**: 需要改进 - 文档不完整

可以通过 `quality_threshold` 配置项设置最低质量要求。

## 故障排除

### 问题: 插件无法加载

**解决方案**:
1. 检查 `.claude-plugin/` 目录结构是否正确
2. 验证 `marketplace.json` 和 `plugin.json` 格式
3. 确认命令文件路径使用相对路径 (`./` 开头)

### 问题: 命令无法执行

**解决方案**:
1. 检查命令文件的 frontmatter 格式
2. 确认 `wiki-config.json` 配置正确
3. 使用 `--verbose` 查看详细错误信息

### 问题: 文档生成不完整

**解决方案**:
1. 检查代码注释是否完整
2. 确认 `exclude_patterns` 没有排除必要文件
3. 使用 `--force` 强制重新生成

### 问题: 模板加载失败

**解决方案**:
1. 检查 `template_dir` 配置路径
2. 确认模板文件存在且格式正确
3. 查看详细错误信息定位问题

## 最佳实践

1. **定期更新文档**: 在代码变更后运行 `/wiki-generate --full`
2. **使用版本控制**: 将生成的文档纳入 Git 管理
3. **自定义模板**: 根据项目需求定制文档模板
4. **质量检查**: 定期检查文档质量分数,保持 ≥ 80 分
5. **排除不必要的文件**: 合理配置 `exclude_patterns` 避免生成无关文档

## 插件结构

```
.claude-plugin/
├── marketplace.json          # Marketplace 配置
├── plugin.json               # 插件清单
├── README.md                 # 本文档
├── commands/
│   └── wiki-generate.md      # Wiki 生成命令
└── templates/                # 文档模板
    ├── base-docs/
    ├── modules/
    └── tech-specific/
```

## 支持

- **问题反馈**: [GitHub Issues](https://github.com/user/repo-wiki/issues)
- **文档**: [项目 README](https://github.com/user/repo-wiki/blob/main/README.md)
- **版本**: 3.0.0

## 许可证

MIT License
